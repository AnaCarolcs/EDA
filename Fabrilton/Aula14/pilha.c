/*Considere o tipo pilha abaixo (que armazena idade de pessoas) e implemente as funções elementares para ela. Indique ainda qual a complexidade mínima (O()) de cada uma das funções.
      struct TPilha{
        int Topo;
        intIdades[5];
      };
*/

void Esvazia(TPilha *Pilha){  //Esvaziar a pilha;
}

bool Vazia(TPilha *Pilha){ //Verificar se a pilha está vazia ou não;
}

bool Empilha(TPilha*Pilha, TElemento *Elemento){ //Empilhar um elemento no topo da pilha;
}

Telemento *Desempilha(TPilha *Pilha){ //Desempilhar um elemento do topo da pilha –esta função retorna o elemento desempilhado;
}

Telemento  *Consulta(TPilha  *Pilha){ //Consulta:  retonar  o  elemento  do  topo  da  pilha  sem  removê-lo;
}

int main(){



  return 0;
}